[project]
name = "nswe"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.41",
  "streamlit-folium>=0.24",
  "folium>=0.19",
  "geopandas>=1.0",
  "shapely>=2.0",
  "geopy>=2.4",
  "pyogrio>=0.10",
]

[tool.streamlit]
# Placeholder section so this file remains the single project config entrypoint.
